// const nota1 = 10;
// const nota2 = 6.5;
// const nota3 = 8;
// const nota4 = 7.5;

// const media = (nota1 + nota2 + nota3 + nota4) / 4;

// posições(índice):    0    1   2   3
const notas1 = [10, 6.5, 8, 7.5];

const media = (notas1[0] + notas1[1] + notas1[2] + notas1[3]) / notas1.length

console.log(media);

// cada dado armazenado em um array pode ser chamado de: elementos, itens e valores
// esses itens são identificados por um índice ou posição que sempre começa do zero

// os arrays são criados em const, let ou var 
// recebem um nome, e seus dados ficam separados por vírgula dentro de colchetes: []
// os arrays aceitam tipos diferentes dentro de uma mesma lista